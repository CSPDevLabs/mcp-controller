apiVersion: v1
kind: Service
metadata:
  name: mcp-controller
  labels:
    app: mcp-controller
spec:
  type: LoadBalancer
  selector:
    app: mcp-controller
  ports:
    - name: http
      port: 8088
      targetPort: 8088
